[project]
name = "arnaldo"
version = "0.1.0"
description = "Nucleo minimo do assistente Arnaldo."
requires-python = ">=3.7"
dependencies = []

[project.scripts]
arnaldo = "arnaldo.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
